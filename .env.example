# Optional local overrides for `docker compose up` (copy to .env). None are required —
# the site is fully static and the APK is served from GitHub Releases.

# Host port the local preview binds on (default 8080).
WEBSITE_PORT=8080
